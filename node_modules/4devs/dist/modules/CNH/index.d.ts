import { ICNH, ICNHValid } from 'lib/interfaces/ICNH';
export declare class CNH {
    static validate({ cnh }: {
        cnh: string;
    }): Promise<ICNHValid>;
    static generate(): Promise<ICNH>;
}
