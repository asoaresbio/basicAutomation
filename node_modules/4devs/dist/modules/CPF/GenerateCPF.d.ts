import { RequestUtil } from '../../utils/RequestUtil';
import { ICPFGenerated } from '../../interfaces/ICPF';
export declare class GenerateCPF {
    requestUtil: RequestUtil;
    execute({ isWithDots, stateCode, }: {
        isWithDots?: boolean;
        stateCode?: string;
    }): Promise<ICPFGenerated>;
}
