import { RequestUtil } from '../../utils/RequestUtil';
import { ICPFValid } from '../../interfaces/ICPF';
export declare class ValidateCPF {
    requestUtil: RequestUtil;
    execute({ cpf }: {
        cpf: string;
    }): Promise<ICPFValid>;
}
