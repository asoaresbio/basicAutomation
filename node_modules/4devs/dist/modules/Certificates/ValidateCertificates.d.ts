import { RequestUtil } from '../../utils/RequestUtil';
import { ICertificateValid } from '../../interfaces/ICertificate';
export declare class ValidateCertificates {
    requestUtil: RequestUtil;
    execute({ certificate, }: {
        certificate: string;
    }): Promise<ICertificateValid>;
}
